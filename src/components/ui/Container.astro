---
interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<div class={['max-w-[1320px] mx-auto px-5 sm:px-8 lg:px-[80px] xl:px-[120px]', className].filter(Boolean).join(' ')}>
  <slot />
</div>
